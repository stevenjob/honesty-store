{
  "name": "@honesty-store/service",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "compile": "tsc",
    "lint": "tslint src",
    "test": "npm run lint && npm run compile",
    "ci": "npm test"
  },
  "dependencies": {
    "http-status": "^0.2.5",
    "winston": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^7.0.0",
    "tslint": "^4.0.0",
    "tslint-microsoft-contrib": "^2.0.14",
    "typescript": "^2.1.0"
  }
}
