{
  "name": "@honesty-store/scripts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "compile": "../node_modules/.bin/tsc",
    "lint": "../node_modules/.bin/tslint  --config ../tslint.json --project tsconfig.json --format stylish --type-check",
    "test": "npm run lint && npm run compile",
    "ci": "npm test"
  },
  "dependencies": {
    "@honesty-store/batch": "file:../batch",
    "@honesty-store/box": "file:../box",
    "@honesty-store/service": "file:../service",
    "commander": "^2.9.0",
    "uuid": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "0.0.2",
    "aws-sdk": "2.22.0"
  }
}
