[
    {
        "name": "container",
        "image": "${image}",
        "memory": 64,
        "essential": true,
        "portMappings": [
            {
                "containerPort": 8080,
                "protocol": "tcp"
            }
        ],
        "logConfiguration": {
            "logDriver": "awslogs",
            "options": {
                "awslogs-group": "${logGroup}",
                "awslogs-region": "eu-west-1",
                "awslogs-stream-prefix": "container"
            }
        }
    }
]