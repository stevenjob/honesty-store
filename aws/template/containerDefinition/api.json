[
    {
        "name": "container",
        "image": "${image}",
        "memory": 64,
        "essential": true,
        "portMappings": [
            {
                "containerPort": 3000,
                "protocol": "tcp"
            }
        ],
        "environment": [
            {
                "name": "BASE_URL",
                "value": "${baseUrl}"
            },
            {
                "name": "JWT_SERVICE_SECRET",
                "value": "${jwtServiceSecret}"
            }
        ],
        "logConfiguration": {
            "logDriver": "awslogs",
            "options": {
                "awslogs-group": "${logGroup}",
                "awslogs-region": "eu-west-1",
                "awslogs-stream-prefix": "container"
            }
        }
    }
]
