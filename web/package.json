{
  "name": "@honesty-store/web",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.7.0",
    "@honesty-store/aws": "file:../aws"
  },
  "dependencies": {
    "react": "^15.4.1",
    "react-bootstrap": "^0.30.7",
    "react-dom": "^15.4.1",
    "react-router": "^3.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build",
    "ci": "CI=true npm test -- --coverage && CI=true npm run build && docker build -t web ."
  }
}
