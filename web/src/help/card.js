import React from 'react';
import Help from './index';

export const HelpCardExpired = () => <Help message="My card seems to have expired" />;

export const HelpCardNoDetails = () => <Help message="I'd like to add a card to my account" />;
