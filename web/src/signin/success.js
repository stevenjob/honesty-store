import React from 'react';
import { Success } from '../layout/alert';

export default () => (
  <Success
    title="Please follow the link in the email to continue"
    subtitle="A magic link was sent to your email address"
  />
);
